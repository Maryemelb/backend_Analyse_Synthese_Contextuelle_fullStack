version: "3.9"
services:
  backend:
    build: .
    image: "summerize_app_backend"
    ports: 
      - "8000:8000"
    environment:
      - DATABASE_NAME=synthese_textuel_db
      - DATABASE_PASSWORD=1111
      - DATABASE_USER=postgres
      - DATABASE_HOST=db
      - DATABASE_PORT=5432
      - NEXT_PUBLIC_BACKEND_API=http://backend:8000
    depends_on:
      - db

  frontend:
    build: ../frontend_analyse_contextuelle
    image: "summerize_app_frontend"
    ports: 
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_BACKEND_API=http://localhost:8000

    depends_on:
      - backend

  db:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 1111
      POSTGRES_DB: synthese_textuel_db
    ports:
       - "5432:5432"
